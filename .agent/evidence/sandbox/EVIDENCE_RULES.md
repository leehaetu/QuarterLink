# HMRC Sandbox Evidence Rules

- HMRC sandbox evidence must come from actual HMRC sandbox API calls.
- Local mock data must never be labelled as HMRC sandbox evidence.
- Every evidence item must link to ticket, run report, endpoint, environment, timestamp, test scenario, request summary, response summary, and pass/fail result.
- No sandbox evidence is created during QL-BOOTSTRAP.
