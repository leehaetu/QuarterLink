# QL-008

Status: DRAFT

## Goal

First HMRC sandbox quarterly update evidence run.

Use the approved QL-007 sandbox foundation to make controlled HMRC sandbox calls and record a clearly labelled sandbox evidence pack. The outcome may be either:

1. a successful first HMRC sandbox quarterly update evidence run, if all required sandbox credentials, test-user authority, scopes, headers, and fixture data are available; or
2. a blocked/readiness report, if credentials, scopes, test-user setup, fraud-prevention data, endpoint details, or fixture values are missing.

Do not fake sandbox evidence. Do not create production evidence. Do not claim HMRC recognition or production readiness.

## Why this ticket exists

QL-003 defined the sandbox-readiness MVP and production-access checklist.
QL-004 created the polished local workflow shell.
QL-005 created the local/static Route B workflow and evidence design.
QL-006 defined the auth, tenant, audit, secrets, token-handling, and fraud-prevention-header architecture.
QL-007 implemented the server-only HMRC sandbox foundation, config validation, request scaffolding, fraud-prevention-header assembly, redaction, tests, and setup docs.

QL-008 is the first controlled attempt to use that foundation against HMRC sandbox services and produce an auditable evidence pack.

## Scope

- Run only against HMRC sandbox endpoints.
- Verify sandbox configuration before any call is attempted.
- Verify OAuth/scopes/test-user setup required for user-restricted Income Tax MTD endpoints.
- Verify fraud-prevention-header readiness.
- Use the Test Fraud Prevention Headers API where available/appropriate before relying on fraud-prevention headers for Income Tax MTD calls.
- Retrieve or confirm the sandbox taxpayer/business context needed for the first individual self-employment quarterly update path.
- Use either:
  - approved Route B/read-only totals fixture values; or
  - an official HMRC sandbox fixture/test scenario.
- Attempt the narrowest safe sandbox call sequence needed to evidence the quarterly update path.
- Record request metadata, response metadata, correlation IDs, timestamps, endpoint names, scopes, status codes, redacted payload summaries, and evidence classification.
- Clearly label all evidence as HMRC sandbox evidence only if it came from actual HMRC sandbox responses.
- Clearly label any local/demo evidence as local/demo only.
- Update the production-access checklist with actual sandbox evidence gathered or blockers found.
- Write a run report.

## Strict Out of Scope

Do not implement:

- production HMRC API calls;
- automatic switching to production endpoints;
- public launch functionality;
- HMRC recognised or production-ready claims;
- full product submission workflow;
- persistent HMRC token storage unless a prior accepted architecture explicitly allows the exact mechanism and QL-008 genuinely needs it;
- database schema/migrations unless explicitly approved;
- full user authentication system;
- multi-user tenancy implementation;
- practice workflows;
- billing;
- final declaration or tax return features;
- spreadsheet upload/parsing;
- bank feeds;
- receipt capture;
- AI categorisation;
- unredacted evidence exports;
- evidence generated from mocked/fake HMRC responses.

## Required source files to read before editing

Codex must read these files before making changes:

- `docs/tickets/QL-008.md`
- `docs/hmrc/SANDBOX_SETUP.md`
- `docs/architecture/QL-006_AUTH_TENANT_AUDIT_SECRETS_HMRC_ACCESS.md`
- `docs/hmrc/FRAUD_PREVENTION_HEADERS_ARCHITECTURE.md`
- `docs/product/SANDBOX_READINESS_MVP.md`
- `docs/hmrc/PRODUCTION_ACCESS_CHECKLIST.md`
- `docs/PROJECT_BRIEF.md`
- `docs/HMRC_WORDING_AND_SCOPE.md`
- `src/server/hmrc/config.ts`
- `src/server/hmrc/client.ts`
- `src/server/hmrc/fraud-prevention.ts`
- `src/server/hmrc/redaction.ts`
- `src/server/hmrc/types.ts`
- existing tests under `tests/`
- `docs/Project files in chatGPT/QuarterLink_01_Product_Source_of_Truth_v2.md`
- `docs/Project files in chatGPT/QuarterLink_03_Current_Live_State_v2.md`
- `docs/Project files in chatGPT/QuarterLink_04_HMRC_Compliance_Matrix_v2.md`
- `docs/Project files in chatGPT/QuarterLink_05_Bridging_Digital_Links_Spec_v2.md`
- `docs/Project files in chatGPT/QuarterLink_06_SaaS_Tenancy_RBAC_Security_v2.md`
- `docs/Project files in chatGPT/QuarterLink_08_Roadmap_Tickets_and_Codex_Prompts_v2.md`
- `.agent/STATUS.md`
- `.agent/QUEUE.md`

## HMRC / GOV.UK guidance to verify

Codex must check the latest official HMRC Developer Hub and GOV.UK guidance where internet access is available, especially:

- HMRC Developer Hub application setup and credentials;
- HMRC user-restricted endpoint OAuth flow;
- Income Tax MTD end-to-end service guide;
- current Income Tax MTD API catalogue, versions, scopes, and sandbox endpoints;
- Business Details API sandbox behaviour if business IDs/income sources are required;
- Obligations API sandbox behaviour if period IDs/obligations are required;
- Self Employment Business / quarterly update API endpoint details if used;
- HMRC fraud prevention headers;
- Test Fraud Prevention Headers API;
- HMRC terms of use / production access requirements;
- Making Tax Digital for Income Tax digital records and digital links;
- quarterly update guidance.

If internet access is unavailable, Codex must not invent HMRC requirements. Mark the ticket blocked with a human-verification item instead of guessing.

## Required preflight gate

Before making any HMRC sandbox call, Codex must run a preflight check and record the result.

The preflight gate must confirm:

- sandbox base URL is configured;
- production base URL is not enabled for this ticket;
- HMRC client ID is configured only if needed;
- HMRC client secret is configured only server-side and never displayed;
- redirect URI is configured if OAuth is attempted;
- required scopes are known from current HMRC docs;
- sandbox test user / authority setup is available;
- fraud-prevention headers can be assembled;
- required client-collected fraud-prevention values are available or a documented HMRC-supported missing-data approach is used;
- server-derived fraud-prevention values are available;
- redaction is applied to all evidence/log output;
- evidence output path is sandbox-only;
- no `.env` or secret file will be committed.

If any preflight item fails, do not make the HMRC call. Create a blocked readiness report instead.

## Implementation boundaries

QL-008 may add limited code only for the sandbox evidence run.

Allowed implementation categories:

- sandbox-only script, command, route, or harness for running the evidence attempt;
- sandbox-only OAuth helper if necessary and if it does not persist tokens beyond the run unless explicitly approved;
- sandbox-only API call wrapper using the QL-007 server HMRC client;
- evidence writer that redacts secrets/tokens and labels evidence correctly;
- static approved fixture values for the first self-employment quarterly update sandbox attempt;
- tests for evidence redaction/classification and sandbox-call preparation;
- docs explaining how to run the sandbox evidence attempt safely.

Not allowed without separate explicit approval:

- persistent token store;
- production endpoint switch;
- database persistence;
- product UI changes;
- full user auth;
- real customer data;
- unredacted evidence bundle;
- running against live taxpayer data;
- committing sandbox credentials or tokens.

## Expected output files

Create or update only files genuinely needed for the narrow QL-008 evidence run.

Likely files include:

- `scripts/hmrc-sandbox-evidence-run.ts` or equivalent, if the repo has a script convention;
- `src/server/hmrc/evidence.ts`;
- `src/server/hmrc/sandbox-fixtures.ts`;
- tests for evidence classification/redaction and sandbox request preparation;
- `docs/hmrc/SANDBOX_EVIDENCE_RUN.md`;
- `docs/hmrc/PRODUCTION_ACCESS_CHECKLIST.md` updates with actual sandbox evidence or blockers;
- `.agent/runs/QL-008-run-001.md`.

Update these files only if genuinely needed:

- `.agent/STATUS.md`
- `.agent/QUEUE.md`
- `docs/tickets/QL-008.md`
- `docs/PROJECT_BRIEF.md`
- `docs/HMRC_WORDING_AND_SCOPE.md`
- `docs/Project files in chatGPT/QuarterLink_03_Current_Live_State_v2.md`
- `docs/Project files in chatGPT/QuarterLink_08_Roadmap_Tickets_and_Codex_Prompts_v2.md`
- `package.json` only if a script is genuinely needed;
- `.env.example` only for placeholder variable names, never real secrets.

## Evidence requirements

Any evidence file or run report must clearly separate:

- local/demo evidence;
- HMRC sandbox evidence;
- production evidence.

For actual HMRC sandbox evidence, record only redacted/safe metadata such as:

- environment: sandbox;
- timestamp;
- endpoint name;
- HTTP method;
- redacted URL/path parameters where needed;
- request ID/correlation ID if returned or generated;
- status code;
- response classification;
- redacted response summary;
- fraud-prevention-header validation status if tested;
- source fixture name/version;
- declaration/evidence wording version if applicable.

Do not record:

- access tokens;
- refresh tokens;
- client secrets;
- raw authorisation codes;
- unredacted NINO if avoidable;
- unredacted HMRC payloads containing sensitive values;
- private keys;
- `.env` contents;
- raw client public IP/port/fingerprint data where inappropriate;
- real customer data.

## Sandbox call sequence

Codex must determine the exact current call sequence from official HMRC docs before implementation.

QL-008 blocker-resolution update on 2026-05-12:

- Human review accepted the blocker-resolution / retry-prerequisites action and its endpoint conclusion. This records the accepted retry planning decision only; Codex must not mark QL-008 `GPT_ACCEPTED`.
- HMRC Developer Hub, the Making Tax Digital for Income Tax service guide, and the HMRC Income Tax MTD changelog confirm that the self-employment period summary endpoints are not the correct path for tax years 2025-26 onwards.
- For a 2025-26 onward first retry, prepare Self Employment Business (MTD) API 5.0 `PUT /individuals/business/self-employment/{nino}/{businessId}/cumulative/{taxYear}`.
- For an explicitly selected 2024-25 or earlier sandbox fixture, use `POST /individuals/business/self-employment/{nino}/{businessId}/period`.
- Changelog entries relied on: 24 July 2024 Self Employment Business API v3.0 period endpoints stop accepting 2025-26 onward data; 11 December 2024 Self Employment Business API v4.0 sandbox creates cumulative period summary endpoints for 2025-26 onwards and marks period endpoints unavailable for 2025-26 onwards; 11 December 2024 Obligations API v3.0 sandbox adds `CUMULATIVE` obligation scenario/data; 24 March 2025 Self Employment Business API v5.0 sandbox adds v5.0 including cumulative period summary support; 30 May 2025 Self Employment Business API v4.0/v5.0 sandbox clarifies `STATEFUL` support for standard cumulative quarterly updates; 24 March 2026 Self Employment Business API v5.0 production still includes Create and Amend a Self-Employment Cumulative Period Summary.
- The retry remains blocked until sandbox credentials, API subscriptions, OAuth/test-user authority, Business Details and Obligations inputs, exact sandbox fixture values, and real `WEB_APP_VIA_SERVER` fraud-prevention inputs are available and the preflight passes.
- This remains Making Tax Digital for Income Tax bridging-only. Do not add bookkeeping, final declaration, production access, database storage, auth, spreadsheet parsing, or product UI work under QL-008.

The intended narrow sequence is:

1. preflight config and fraud-header readiness;
2. OAuth/scopes/test-user readiness;
3. optional Test Fraud Prevention Headers API validation where available/appropriate;
4. retrieve/confirm business details or income source identifiers if required;
5. retrieve/confirm obligations/period ID if required;
6. perform the first self-employment quarterly update sandbox call only if all required inputs are confirmed;
7. record redacted sandbox evidence;
8. update checklist/run report.

If the exact current endpoint/version/scope/payload is uncertain, stop and record the blocker. Do not guess.

## Route B fixture requirements

The fixture must represent the first sandbox-readiness slice:

- individual taxpayer;
- one self-employment income source;
- one quarterly update period;
- read-only Route B linked summary totals;
- no in-app figure editing;
- no final declaration or tax-return calculation.

Fixture values must be clearly marked as sandbox/test values and must not represent real customer data.

## Tests

Add or run tests for:

- evidence classification: local/demo vs sandbox vs production;
- evidence redaction;
- no token/secret leakage in evidence output;
- preflight failure behaviour;
- sandbox request preparation;
- fraud-prevention-header validation/required-field behaviour where practical.

## Checks

Run:

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- `npm audit --audit-level=moderate`
- `git diff`
- `git diff --check`.

Also confirm:

- changed paths are appropriate for QL-008;
- no real secrets are included;
- no `.env` file is committed;
- no production HMRC calls are enabled;
- no unredacted HMRC token/secret/fraud metadata is logged or stored;
- no product UI changed unless explicitly justified;
- no database/schema/migrations were added without explicit approval;
- no HMRC recognised or production-ready claim was added.

## npm audit rule

If `npm audit --audit-level=moderate` still reports the known Next/PostCSS moderate advisories, record the result but do not run `npm audit fix --force` inside QL-008 unless explicitly instructed by the human reviewer. A breaking dependency fix must be handled as a separate dependency/security ticket.

## Run report

Create `.agent/runs/QL-008-run-001.md` containing:

- summary of work completed;
- files changed;
- HMRC/GOV.UK sources checked;
- exact endpoint versions/scopes used or blocked;
- preflight result;
- whether the Test Fraud Prevention Headers API was used;
- whether any HMRC sandbox calls were made;
- if calls were made: redacted metadata and evidence location;
- if calls were blocked: precise blocker list and required human action;
- evidence classification result;
- tests/checks run and results;
- npm audit result;
- human-verification items;
- deferred implementation items;
- whether product UI changed;
- whether any production access/recognition claim was added;
- exact next recommended ticket.

## Approval

Human approval is required before running this ticket.

Codex must not mark this ticket `GPT_ACCEPTED`.

Human review is still required after Codex completes the ticket.

## Human Acceptance Handoff

When human review is accepted, Codex must update the project control state, run the required checks, review `git diff`, then commit and push the accepted QL-008 changes.

Required acceptance sequence:

1. Update control/status files to mark QL-008 as human accepted / GPT_ACCEPTED using the repo's existing accepted-status wording.
2. Confirm the next active ticket recommended by the QL-008 run report.
3. Run:
   - `npm run typecheck`
   - `npm run lint`
   - `npm test`
   - `npm run build`
   - `npm audit --audit-level=moderate`
   - `git diff`
   - `git diff --check`
4. Confirm the diff is limited to the accepted QL-008 work and control/status updates.
5. Confirm no unrelated files, secrets, build artefacts, `node_modules`, `.env` files, or local-only files are included.
6. Confirm any evidence committed is redacted and safe to commit.
7. Commit the accepted changes with a clear message, for example:
   - `git commit -m "Accept QL-008 sandbox evidence run"`
8. Push the commit to the current branch.
9. Return a short summary including checks passed, audit result, evidence classification, files committed, commit hash, branch pushed, and next recommended ticket.

Do not start the next ticket during the QL-008 acceptance handoff.
