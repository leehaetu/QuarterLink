# QL-002

Status: COMMITTED

## Goal

Close the control-state drift after QL-001 and the project-file consolidation, then reconcile the ticket sequence so the next work can proceed in the right order.

This is a documentation and control-system ticket only. It records that QL-001 and the project-file consolidation were accepted by the human, keeps Codex from marking anything `GPT_ACCEPTED`, and replaces the old draft sequence with a polished sandbox-readiness MVP path.

## Scope

- Mark QL-001 as committed after human acceptance.
- Mark the project-file consolidation as completed and committed.
- Update `.agent/STATUS.md` and `.agent/QUEUE.md`.
- Update the v2 current-live-state, workflow, index, and roadmap files.
- Replace thin draft tickets with a coherent next-ticket sequence.
- Keep QL-003 as the recommended next ticket: a short sandbox-readiness MVP target and HMRC production-access checklist.
- Create a run report for this control sync.

## Out of Scope

- Product feature implementation.
- HMRC OAuth.
- HMRC API calls.
- HMRC sandbox evidence.
- Production approval or recognition claims.
- Authentication.
- Database storage.
- Spreadsheet upload, import, parsing, mapping, or category extraction.
- Quarterly update payload submission.
- Billing or platform admin implementation.
- Marking any work `GPT_ACCEPTED`.

## Acceptance Criteria

- QL-001 is recorded as `COMMITTED`, not `GPT_ACCEPTED`.
- Project-file consolidation is recorded as completed and committed.
- The live-state file no longer says project-pack cleanup is pending.
- The ticket queue no longer points QL-002 at tax-regime adapter implementation.
- Draft tickets make the next sandbox-readiness and product-building steps clear.
- Checks pass.
- `.agent/runs/QL-002-run-001.md` is created.

## Checks to Run

- `node -v`
- `npm run typecheck`
- `npm run lint`
- `npm run build`

## Approval

Human approval to perform this control sync and commit it was given on 2026-05-12.

Codex must not mark this ticket `GPT_ACCEPTED`.
